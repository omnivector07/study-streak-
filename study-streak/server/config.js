// Central place for app-wide constants.
// Keeping this separate makes it trivial to later expose per-user
// configurable goals without touching business logic elsewhere.
module.exports = {
  DAILY_GOAL_MINUTES: 60
};
